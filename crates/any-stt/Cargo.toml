[package]
name = "any-stt"
version = "0.1.0"
edition = "2021"

[dependencies]
thiserror = "2"

[target.'cfg(any(target_os = "linux", target_os = "android"))'.dependencies]
libc = "0.2"

[features]
cuda = []
metal = []
coreml = []
vulkan = []
nnapi = []
